# Core API
NEXT_PUBLIC_CORE_API_URL="http://localhost:3000"

# Supabase Auth (email/password + TOTP MFA) — same project as core-api Postgres
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[ANON_KEY]"

# Unused by admin Auth (kept for shared @repo packages if needed)
NEXT_PUBLIC_POLLAR_API_KEY=""
NEXT_PUBLIC_TRUSTLESS_WORK_API_KEY=""
NEXT_PUBLIC_USE_MAINNET=false
